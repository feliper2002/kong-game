import 'package:flutter/material.dart';

class Kong extends StatelessWidget {
  const Kong({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset('assets/sprites/kong/kong.png', height: 60);
  }
}
